# Direct dependencies pinned to the release verification environment.
# Upgrade deliberately and rerun unit/integration tests and pip-audit.
# Notion stays on 2.2.1: later SDK defaults change the database API contract;
# 3.x also changes error/transport interfaces used by safe submission retries.
# See docs/RELEASE_VERIFICATION.md for compatibility evidence.
pdfplumber==0.11.10
notion-client==2.2.1
python-dateutil==2.9.0.post0
rich==15.0.0
inquirer==3.4.1
python-dotenv==1.2.3
langgraph==1.2.12
openai==3.18.0
pydantic==2.13.5
httpx==0.28.1
requests==2.34.2
